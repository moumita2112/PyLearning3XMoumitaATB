# moumita = [1, 2, "tree", "yes", 2.45]
# moumita.sort()  # Sorting cant be performed with different data types


squares = [1, 2, 8, 9, 0]
if not False:  # Interview question: not operator works only with boolean
    print("Empty")
else:
    print("Not Empty")
